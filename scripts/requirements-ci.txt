PyYAML>=6.0.3
python-frontmatter>=1.0.0
pytest>=8.4.2
pytest-cov>=7.1.0
requests>=2.28.0
beautifulsoup4>=4.12.0
qrcode>=8.2
# fontTools reads the shipped woff2 cmap in scripts/tests/test_font_glyph_coverage.py
# (the Hangul glyph-coverage gate). The [woff] extra pulls brotli, needed to
# decompress woff2. Same floor build.sh pip-installs for the generator.
fonttools[woff]>=4.55.0
