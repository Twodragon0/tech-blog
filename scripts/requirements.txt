requests>=2.34.2
certifi>=2026.7.22
beautifulsoup4>=4.15.0
python-frontmatter>=1.3.0
lxml>=6.1.3
tweepy>=4.17.0
PyYAML>=6.0.3
Pillow>=11.3.0,<12.0  # moviepy 2.x requires pillow<12.0; ceiling prevents transitive break
python-dotenv>=1.2.3

# RSS 피드 파싱 (뉴스 수집)
feedparser>=6.0.14

# Google OAuth 2.0 및 Gemini API (선택적)
google-auth>=2.58.0
google-auth-oauthlib>=1.4.1
google-auth-httplib2>=0.4.2
google-generativeai>=0.8.6

# TTS (선택적) — generate_post_images.py:106 `from gtts import gTTS`
gTTS>=2.5.4
edge-tts>=7.2.8
#
# Coqui `TTS` was removed here on 2026-09-16. It was dead: nothing in this repo
# imports it (`import TTS` / `from TTS`: 0 hits) and its only documented consumer,
# generate_enhanced_audio.py, moved to the online-course repo in cfe0d82d —
# scripts/README.md already points there. The audio path that remains is gTTS,
# a different package with no Python cap.
#
# It was not free to keep. Coqui TTS declares `Requires-Python >=3.9.0, <3.12`,
# which is the ONLY Python constraint this file had, and on 3.11 it pulled the
# install from 81 to 191 packages — ~639 MB of wheels, 554 MB of it torch — and
# held numpy at 1.26.4 instead of 2.5.3 for everything else in the environment.
# The generate-images.yml `Install dependencies` step took 150s (run
# 34919610544, 2026-09-15).
#
# Re-adding it means re-adding a <3.12 ceiling for the whole file. Don't, unless
# something here actually imports it.

# 비디오/SVG 처리 (선택적)
moviepy>=2.2.1
cairosvg>=2.9.0,<3.0
qrcode>=8.0.0

# AWS 아키텍처 다이어그램 생성
diagrams>=0.25.1
graphviz>=0.20.3
