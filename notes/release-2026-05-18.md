# 2026-05-18 세션 — L25 파이프라인 + L22 QR 회귀 수정

## 세션 개요
- 범위: L25-single 렌더러 정식화, L22 dispatch QR 회귀 수정, 골든 스냅샷 확장, CI drift gate
- 결과: 9개 커밋, 모두 main 푸시 완료, CI 통과
- 푸시 범위: `c3787381..59558552` (직전 USE_L22_ULTRA 강제화 이후 연속 작업)

## 커밋별 상세

### 1. `3781d968` test(news): L22 ultra golden snapshots + structural invariants for 2026-04-29/04-30
- 신규: `scripts/tests/test_l22_golden_snapshots.py` (152 LOC, 10 테스트)
- 04-29 / 04-30 SVG 2개를 골든 기준으로 등록 — 바이트 동등성 + 4종 구조 불변식 (QR 마커 `translate(1080,504)`, 파일 크기 ≥50KB, 3개 테마 액센트 모두 존재, `<title>` 비어있지 않음)
- 렌더러 결정론성 확인 후 byte-equality 채택

### 2. `51a32f47` feat(news): L25-single renderer pipeline + drift gate (forward-looking)
- 신규 파일 4개 (799 LOC): `scripts/lib/svg_l25_single.py` (246), `scripts/upgrade_l25_cover.py` (312), `scripts/tests/test_svg_l25_single.py` (241), `_data/l25_covers/.gitkeep`
- L22/L20/rollup과 동일한 CLI 인터페이스 (`--spec/--all/--since/--check/--dry-run`)
- 6 visual_id (network_nodes, code_bars, shield, hub_spoke, supply_chain_pipe, lock_cve) — L20 hero 시각 빌더 재사용
- `--all --check` empty dir 시 exit 0
- 28개 기존 grandfathered 2025 SVG는 건드리지 않음

### 3. `b8960386` test(news): extend L22 golden snapshots to 2026-05-01~10 (12 specs total)
- `test_l22_golden_snapshots.py`에 10개 스펙 추가 (parametrize 2→12)
- 60 tests, 0.47s
- 테마 분포: 11/12 specs {red, amber, green}, 1/12 spec {red, amber, blue}

### 4. `88da4389` ci(svg-lint): add L25 single drift gate alongside L22/rollup checks
- `.github/workflows/svg-lint.yml`에 step 추가: `python3 scripts/upgrade_l25_cover.py --all --check`
- `paths:` trigger에 `scripts/upgrade_l25_cover.py`, `scripts/lib/svg_l25_single.py`, `_data/l25_covers/**.yml` 추가
- L20 drift gate trigger paths 누락은 pre-existing (out of scope)

### 5. `50e1943a` fix(news): pass filename to extract_three_stories in L22 dispatch — restores QR + real keywords on 05-13~18
- **회귀 수정**: `render_l22_svg_string` / `generate_l22_digest_svg`가 `extract_three_stories(title, excerpt)` 호출 시 `filename=` 누락
- 한국어 전용 자동 발행 게시물(05-13~18)이 placeholder 풀 fallback `["Security Update", "Threat Analysis", "Patch Advisory"]`로 떨어짐
- 2-line 수정 + 회귀 가드 `TestKoreanTitleFilenameExtraction` 추가
- 6개 SVG 재렌더링 + 30개 raster variant 재생성

### 6. `27cccce0` feat(news): L25-single rich variant + 3 prototype specs (Cloudflare/FinOps/K8s)
- `svg_l25_single.py` +219 LOC: `_visual_frame`, `_accent_badge`, `_accent_pulse_strip`, `_detail_line` 헬퍼 + `_CHIP_TONES` 팔레트 + dict-form chip 지원
- `--output PATH` 플래그 추가 (preview 렌더링용)
- 3개 prototype YAML spec (Cloudflare Outage, AWS FinOps W3, K8s Minikube)
- 43 tests pass (tmp-dir isolation으로 real spec 영향 차단)

### 7. `a164e5c1` feat(news): add outage_timeline + k8s_topology visuals — domain-fit replacements for Cloudflare/K8s prototypes
- 2개 신규 visual primitive:
  - `outage_timeline`: 5-phase timeline (DETECT/DIAGNOSE/MITIGATE/RESOLVE/POSTMORTEM) + BLAST RADIUS concentric arcs (GLOBAL/US-EAST/EU/ASIA)
  - `k8s_topology`: api-server CONTROL PLANE 중앙 + 6 satellite (POD/SVC/NS/CRD/CM/NODE) + single-node 배지
- Cloudflare YAML `shield` → `outage_timeline`, K8s YAML `network_nodes` → `k8s_topology`
- 47 tests pass

### 8. `034d1c0f` feat(images): promote 3 L25-single prototype covers to production
- 3개 prototype을 production .svg로 승격 (grandfathered hand-crafted SVG 덮어쓰기)
- Cloudflare YAML slug 케이싱 수정 (`What` → `what`, 파일시스템 정합)
- 19 파일 변경 (3 SVG + 15 variant + 1 YAML)
- Drift gate 3/3 OK
- 나머지 25개 2025 cover는 grandfathered 보존 (보수적 옵션 b)

### 9. `59558552` feat(docs): L25 visual gallery contact sheet — author reference for visual_id picks
- `assets/l25-visual-gallery.html` (63 KB)
- 8개 visual_id × inline SVG + 캡션 + YAML 사용 예시
- Dark theme, CSS Grid 2-col 데스크탑/1-col 모바일, JS 의존성 없음
- 추후 spec 작성 시 visual 선택 보조용

## 미해결 / Follow-up
- L20 hero drift gate `paths:` trigger 누락 (pre-existing): `upgrade_l20_cover.py` / `svg_l20_generator.py` / `_data/l20_covers/**` 추가 필요
- L25 visual gallery 정식 nav 노출 (현재 dev only)
- 25개 grandfathered 2025 cover의 향후 처리 방침 미결정 (`(a)` YAML 시드 vs `(b)` 영구 grandfather)
- `.omc/plans/cover-upgrade-consolidation.md` (537 LOC) — 4개 upgrade_*_cover.py 공통화 계획, 31% 중복, 의사결정 대기

## 검증
- 모든 pre-commit: `pytest scripts/tests/` 1638+ tests pass (~3s)
- L22 + L25 suites: 101 + 47 tests pass
- CI: `Action Pin Check`, `Jekyll site CI`, `CodeQL`, `Deploy Jekyll to GitHub Pages` all green
- Drift gate: L22 (46/46), rollup (6/6), L20 (15/15), L25 (3/3) all OK
