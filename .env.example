# LinkedIn API Credentials
# Copy this file to .env and fill in your actual values
# NEVER commit .env file to git!

# LinkedIn OAuth 2.0 Credentials
LINKEDIN_CLIENT_ID=your_client_id_here
LINKEDIN_CLIENT_SECRET=your_client_secret_here

# LinkedIn OAuth Redirect URI (must match LinkedIn Developer Portal settings)
LINKEDIN_REDIRECT_URI=http://localhost:8000/auth/linkedin/callback

# LinkedIn Access Token (obtained after OAuth flow)
# This will be set automatically after authentication
LINKEDIN_ACCESS_TOKEN=

# LinkedIn Person ID (your LinkedIn profile ID)
# This will be set automatically after authentication
LINKEDIN_PERSON_ID=

# Site Configuration
SITE_URL=https://tech.2twodragon.com

# Other SNS API Keys (optional)
TWITTER_API_KEY=
TWITTER_API_SECRET=
TWITTER_ACCESS_TOKEN=
TWITTER_ACCESS_SECRET=

FACEBOOK_PAGE_ID=
FACEBOOK_ACCESS_TOKEN=

# ============================================
# Audio/Video Generation API Keys
# ============================================

# ElevenLabs API (Required for audio generation)
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here
ELEVENLABS_VOICE_ID=your_voice_id_here

# DeepSeek API (Optional, for script generation)
DEEPSEEK_API_KEY=sk-your_deepseek_api_key_here

# Gemini API (Optional, for script generation and improvement)
GEMINI_API_KEY=your_gemini_api_key_here

# ============================================
# Google Cloud OAuth 2.0 (Recommended)
# ============================================
# Use service account for secure authentication
# Download service account key from Google Cloud Console
# See scripts/GEMINI_OAUTH_SETUP.md for detailed instructions

# Google Cloud Service Account Credentials
# Path to the service account key JSON file
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your-service-account-key.json

# Google Cloud Project ID
# Replace with your actual project ID
GOOGLE_CLOUD_PROJECT=your-project-id-here

# Enable OAuth 2.0 for Gemini API (recommended)
USE_GEMINI_OAUTH=true

# ============================================
# API Selection Strategy
# ============================================

# Prefer Gemini AI Pro (default: true)
PREFER_GEMINI=true

# Use Gemini CLI (default: false, requires npm install -g @google/gemini-cli)
USE_GEMINI_CLI=false

# Use DeepSeek for script generation (default: true)
USE_DEEPSEEK_FOR_SCRIPT=true

# Use Gemini for script generation (default: true)
USE_GEMINI_FOR_SCRIPT=true

# Use Gemini for script improvement (default: true)
USE_GEMINI_FOR_IMPROVEMENT=true

# Enable caching (default: true)
ENABLE_CACHING=true

# ============================================
# Upstash Redis (Rate Limiting & Caching)
# ============================================
# Free Tier: 500K commands/month, 256MB storage
# Get credentials from: https://console.upstash.com/
# 
# Security Best Practices:
# - Use REST API (HTTPS) for serverless environments
# - Enable TLS encryption (default on Upstash)
# - Rotate tokens periodically

# Upstash Redis REST API URL
# Format: https://<region>.upstash.io
UPSTASH_REDIS_REST_URL=https://your-region.upstash.io

# Upstash Redis REST API Token
# Keep this secret! Never expose in client-side code
UPSTASH_REDIS_REST_TOKEN=AXxxxxxxxxxxx

# ============================================
# Prisma Postgres (via Prisma Accelerate)
# ============================================
# Free Tier: 512MB storage, 100K queries/month
# Connection pooling included with Accelerate
# 
# Setup:
# 1. Create database at https://console.prisma.io/
# 2. Enable Accelerate for connection pooling
# 3. Use prisma:// URL for pooled connections

# Prisma Accelerate Connection URL (for pooled connections)
# Format: prisma://accelerate.prisma-data.net/?api_key=xxx
DATABASE_URL=prisma://accelerate.prisma-data.net/?api_key=your_api_key

# Direct Database URL (for migrations only, not for production queries)
# Format: postgres://user:password@host/database
DIRECT_URL=postgres://user:password@your-host.neon.tech/your-database

# ============================================
# Cost Optimization Settings
# ============================================

# Enable Upstash Redis for rate limiting (recommended for production)
# If false, uses in-memory rate limiting (not recommended for multi-instance)
USE_UPSTASH_RATELIMIT=true

# Rate limiting configuration
# Anonymous users: requests per minute
RATELIMIT_ANONYMOUS_RPM=10

# Authenticated users: requests per minute
RATELIMIT_AUTHENTICATED_RPM=30
